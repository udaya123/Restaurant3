<%= form_for(rest_cat) do |f| %>
  <% if rest_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rest_cat.errors.count, "error") %> prohibited this rest_cat from being saved:</h2>

      <ul>
      <% rest_cat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rsetaurant_id %>
    <%= f.number_field :rsetaurant_id %>
  </div>

  <div class="field">
    <%= f.label :category_id %>
    <%= f.number_field :category_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
